#!/bin/sh

# Prints the DND status.

case $BLOCK_BUTTON in
	1) notify-send "Playing now $title" ;;
esac

status="$(dunstctl is-paused)"

if [ "$status" = "false" ]; then
	icon="󰚘"
else
	icon="󰚙"
fi

echo "$icon"
